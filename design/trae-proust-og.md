# Trae Task: Proust OG Image — Phase 1\n\n**Bounded to**: OG Image generation + Prisma model only.\n**Out of scope**: Proust UI, card editor, onboarding, download button.\n\n---\n\n## Step 1: Add ProustAnswer Model\n\n**File**: `prisma/schema.prisma`\n\nAdd after existing model definitions:\n\n```prisma\nmodel ProustAnswer {\n  id        String   @id @default(cuid())\n  userId    String   @map(\"user_id\")\n  question  String\n  answer    String\n  source    String   @default(\"proust\")\n  isPublic  Boolean  @default(true)\n  createdAt DateTime @default(now())\n\n  user User @relation(fields: [userId], references: [id], onDelete: Cascade)\n\n  @@index([userId, createdAt(sort: Desc)])\n  @@map(\"proust_answer\")\n}\n```\n\n**Add relation to User model**:\n```prisma\nproustAnswers ProustAnswer[]\n```\n\nThen run: `prisma db push`\n\n---\n\n## Step 2: Create OG Image Route\n\n**File**: `src/app/api/og/proust/[username]/route.tsx`\n\n- Use `next/og` `ImageResponse` (dimensions: 1200×630)\n- Fetch user's latest public `ProustAnswer` via `Profile.username` join\n- Render `<ProustCard>` component with:\n  - Warm off-white background (#FAFAF8)\n  - Top: OASISBIO wordmark (text only, no logo image)\n  - Question: small sans-serif, muted gray, uppercase, letter-spaced\n  - Answer: large serif (Georgia/Times), dark, max 3 lines, overflow ellipsis\n  - Footer: username · identity universe · url\n- Satori constraints: NO `gap`, NO `box-shadow`, NO `position: absolute` — use margin + padding + flex only\n- Return `Cache-Control: public, max-age=86400`\n- If no answer found: return a brand-only fallback card (OasisBio wordmark + tagline)\n\n---\n\n## Step 3: Create ProustCard Component\n\n**File**: `src/components/proust/ProustCardOG.tsx`\n\nThis component is **only for Satori rendering** (server component, no interactivity).\n\nExport named component `<ProustCardOG>` that accepts:\n```ts\n{\n  username: string\n  question: string\n  answer: string\n  profileUrl: string\n}\n```\n\nAll styling via inline style objects (required by Satori).\n\n---\n\n## Step 4: Create Data Fetch Function\n\n**File**: `src/lib/proust.ts`\n\n```ts\nexport async function getUserLatestProustAnswer(username: string) {\n  // Join Profile → User → ProustAnswer\n  // Return latest public answer or null\n}\n```\n\nUse Prisma client from `~/lib/prisma`.\n\n---\n\n## Step 5: Integrate into Bio Page\n\n**File**: `src/app/bio/[username]/page.tsx`\n\nAdd to `generateMetadata`:\n```ts\nopenGraph: {\n  images: [`/api/og/proust/${params.username}`],\n}\n```\n\n---\n\n## Acceptance Criteria\n\n1. `GET /api/og/proust/ceaserzhao` returns a valid PNG (1200×630)\n2. Card contains: question text + answer text + username + \"identity universe\" + URL\n3. Satori renders without errors (no unsupported CSS properties)\n4. `/bio/ceaserzhao` page meta includes the OG image URL\n5. Fallback card renders correctly when user has no Proust answers yet\n6. `prisma db push` succeeds with no errors\n