# 2026-05-16 工作日志

## OasisBio 上线前 Bug 修复 + OAuth 验证 + 样式优化

### 修复的问题（3 commits）

#### Commit 1: `eb44c81` — fix(auth): rewrite OAuth callback as server route handler
**最关键的 bug：**
- 旧的 `src/app/auth/callback/page.tsx`（client component）用 `useEffect` 调 `exchangeCodeForSession`
- PKCE `code_verifier` 存在 HttpOnly cookie 里，client component 无法可靠读取
- **修复**：删除 page.tsx，新建 `src/app/auth/callback/route.ts`（Route Handler），在 server side 用 `createClient()` 完成 code exchange
- OAuth 错误改为重定向到 `/auth/login?error=xxx`，login 页面新增 `useEffect` 展示 OAuth 错误
- `confirm/page.tsx`：修复 `verifyOtp` 缺少 email 参数 bug，改用 `token_hash + type` 模式（Supabase PKCE 新流程）
- `OAuthButtons.tsx`：将 CDN 图片替换为内联 SVG（Google 彩色 G + GitHub Octocat）

#### Commit 2: `0e69786` — feat(brand): add favicon SVG, web manifest, complete OG metadata
- 新增 `public/favicon.svg`（紫色品牌图标，圆形+层叠条）
- 新增 `public/site.webmanifest`（PWA manifest，theme #7C3AED）
- `layout.tsx` 添加 icons 配置、manifest 引用、metadataBase、og.url

#### Commit 3: `c9f7fe9` — fix: resolve build errors blocking production deployment
- `SuccessModal.tsx`：`Wechat` lucide icon 不存在 → 改为 `MessageCircle`
- `globals.css`：`@apply clip-rect-0`（无效 Tailwind 类）→ 原生 `clip: rect(0,0,0,0)`
- `cloudflare-r2.ts`：不再 import `server-only` 的 config.ts，改为直接读 env（解决 client bundle 打包错误）

### 验证结果
- TypeScript: 0 errors
- next build: ✅ BUILD OK（无 error，仅 1 个 NFT warning 属 turbopack 已知问题）
- 全部推送到 GitHub main 分支
